{
    "version": 2,
    "builds": [
        {
            "src": "${INPUT_SRC}",
            "use": "@vercel/static-build",
            "config": { "outputDirectory": "${OUTPUT_DIR}" }
        }
    ],
    "env": {
        "INPUT_SRC": "src/**/*",
        "OUTPUT_DIR": "build"
    }
}
