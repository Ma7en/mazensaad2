{
    "version": 2,
    "builds": [
        { "src": "build/**/*.html", "use": "@now/static" },
        { "src": "build/**/*", "use": "@now/static" }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/$1" }]
}
